{% extends "BibliothequeBundle:Default:layout.html.twig" %}

{% block module %}
    <h1>Livre edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('livre') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
